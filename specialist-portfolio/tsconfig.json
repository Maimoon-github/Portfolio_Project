{
  // Root TypeScript configuration – references project-specific configs.
  // This file enables TypeScript project references, allowing the app and
  // Node.js configurations to be checked and built separately.
  "files": [],
  "references": [
    { "path": "./tsconfig.app.json" }, // Browser/React app config
    { "path": "./tsconfig.node.json" } // Node.js / build tooling config
  ]
}