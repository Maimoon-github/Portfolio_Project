export function ProjectHero({ title, role, duration, heroImage, liveUrl, repoUrl }: { title?: string, role?: string, duration?: string, heroImage?: string, liveUrl?: string, repoUrl?: string }) {
    return <div className="rounded border p-4">Project Hero placeholder</div>
}